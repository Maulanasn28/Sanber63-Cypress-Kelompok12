class SignIn {
    email = '#email'; // Selector untuk email field
    password = '#pass'; // Selector untuk password field
    loginButton = '#send2'; // Selector untuk tombol login
  }
  
  export default new SignIn();
  